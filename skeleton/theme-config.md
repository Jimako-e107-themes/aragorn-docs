# theme_config.php

Skeleton only for now: the theme admin preferences class with no
preferences defined yet.

Planned preferences:

* `auth_bgimage` — auth splash image (read in PHP by the fpw/membersonly
  templates, exposed to layouts via a `{THEME_AUTH_BGIMAGE}` shortcode —
  see [Auth pages](../standalone/auth-pages.md) for why two access paths
  are needed).
* Home layout content preferences as the front page develops.
